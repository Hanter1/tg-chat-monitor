# Android APK dependencies (Chaquopy pip).
# Keep in sync with ../requirements.txt where possible.
# pyaes / pydantic-core come from android/wheels (see wheels/README.md).
aiogram>=3.4.0,<4
telethon>=1.34.0,<2
sqlalchemy>=2.0.0,<3
aiosqlite>=0.19.0
python-dotenv>=1.0.0
pyaes==1.6.1
pydantic==2.13.4
pydantic-core==2.46.4
# Pin to Chaquopy-provided Android wheel (avoids newer pure deps without wheels).
aiohttp==3.10.10
