# Android APK dependencies (Chaquopy pip).
# Keep in sync with ../requirements.txt — avoid packages without Android wheels.
aiogram>=3.4.0,<4
telethon>=1.34.0,<2
sqlalchemy>=2.0.0,<3
aiosqlite>=0.19.0
python-dotenv>=1.0.0
