# Android native wheel build helpers
#
# Adapted from https://github.com/ririv/android-wheels (MIT) for building
# Rust/maturin packages (notably pydantic-core) as Chaquopy-compatible
# `android_*` wheels in CI.
